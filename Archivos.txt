Estos son los comandos para descargar todas las dependencias para backend y frontend

1.-Backend:
    1.-este comando se ejecuta en terminal en larpeta raiz:
        go get github.com/felixge/httpsnoop github.com/gorilla/handlers github.com/gorilla/mux github.com/jackc/pgpassfile github.com/jackc/pgservicefile github.com/jackc/pgx/v5 github.com/jackc/puddle/v2 github.com/jinzhu/inflection github.com/jinzhu/now gorm.io/driver/postgres gorm.io/gorm
2.-frontend 
    1.-este comando se ejecuta en terminal en la carpeta donde esta react que es CrudCovid 
    es decir esta la direccion en donde guardaste el proyecto por ejemplo:
     C:\Users\tu_usuario\Desktop\CRUD-GO\CrudCovid>(si en dado caso que creaste otra carpeta ya solo la agregas)
     en mi caso es en escritorio, se creo una carpeta y dentro se hizo la carpeta de "CrudCovid"

     este es el comando:
        npm install axios chart.js date-fns react react-chartjs-2 react-dom react-modal react-router-dom
        npm install --save-dev @types/react @types/react-dom @vitejs/plugin-react eslint eslint-plugin-react eslint-plugin-react-hooks eslint-plugin-react-refresh vite
